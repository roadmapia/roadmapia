# Roadmapia — App (learnai/)

> Este directorio es el código fuente de la app. El CLAUDE.md principal con toda la documentación está en `../CLAUDE.md` (un nivel arriba).

**Ruta local**: `/home/ruben/proyectos/roadmapia/learnai/`
**Arrancar**: `.venv/bin/uvicorn main:app --reload`
**Deploy VPS**: `cd /var/www/roadmapia && git pull && sudo systemctl restart roadmapia`
